@using EntityLayer.Concrete
@model List<Company>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Company List</h4>

            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Şirket İsmi</th>
                            <th>Sil</th>
                            <th>Güncelle</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.CompanyID</td>
                                <td>@item.CompanyName</td>
                                <td><a href="/GamePanel/Company/DeleteCompany/@item.CompanyID" class="btn btn-outline-danger">Sil</a></td>
                                <td><a href="/GamePanel/Company/EditCompany/@item.CompanyID" class="btn btn-outline-primary">Güncelle</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/GamePanel/Company/AddCompany/" class="btn btn-outline-primary">Şirket Ekle</a>
            </div>
        </div>
    </div>
</div>


