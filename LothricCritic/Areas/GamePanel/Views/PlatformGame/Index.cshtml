@using EntityLayer.Concrete
@model List<PlatformGame>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">PlatformGame List</h4>

            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Platform ID</th>
                            <th>Oyun ID</th>
                          @*  <th>Sil</th>
                            <th>Güncelle</th>*@
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.PlatformID</td>
                                <td>@item.GameID</td>
                                @*<td><a href="/GamePanel/PlatformGame/DeletePlatformGame/@item.PlatformID" class="btn btn-outline-danger">Sil</a></td>
                                <td><a href="/GamePanel/PlatformGame/EditPlatformGame/@item.PlatformID" class="btn btn-outline-primary">Güncelle</a></td>*@
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/GamePanel/PlatformGame/AddPlatformGame/" class="btn btn-outline-primary">Platformla Oyunu Birleştir</a>
            </div>
        </div>
    </div>
</div>

